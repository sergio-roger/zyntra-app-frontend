name: Frontend CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test-and-lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Build project (production build check)
        run: npm run build

  build-and-push-hub-prod:
    needs: test-and-lint
    if: gitea.event_name == 'push' && gitea.ref_name == 'main'
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.HARBOR_URL }}
          username: ${{ secrets.HARBOR_USER }}
          password: ${{ secrets.HARBOR_PASS }}

      - name: Login GCP Artifact Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.GCP_REGION }}-docker.pkg.dev
          username: _json_key
          password: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}

      - name: Get short SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITEA_OUTPUT

      - name: Determine tags
        id: meta
        run: |
          SHA=${{ steps.vars.outputs.sha_short }}
          REGISTRY=${{ secrets.HARBOR_URL }}
          PROJECT=zyntra
          TAGS="$REGISTRY/$PROJECT/marketing-frontend:latest,$REGISTRY/$PROJECT/marketing-frontend:prod-$SHA"
          echo "tags=$TAGS" >> $GITEA_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          provenance: false
          build-args: |
            VITE_API_URL=${{ secrets.VITE_API_URL }}

      - name: Mirror latest → GCP Artifact Registry
        run: |
          SOURCE="${{ secrets.HARBOR_URL }}/zyntra/marketing-frontend:latest"
          TARGET="${{ secrets.GCP_REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/${{ secrets.GCP_REPOSITORY }}/marketing-frontend:latest"

          docker pull $SOURCE
          docker tag $SOURCE $TARGET
          docker push $TARGET

          echo "✅ Mirror prod → $TARGET"

      - name: Trigger Dokploy Deployment
        run: curl -X POST "${{ secrets.DOKPLOY_WEBHOOK_URL }}"

  build-and-push-hub-dev:
    needs: test-and-lint
    if: gitea.event_name == 'push' && gitea.ref_name == 'develop'
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.HARBOR_URL }}
          username: ${{ secrets.HARBOR_USER }}
          password: ${{ secrets.HARBOR_PASS }}

      - name: Login GCP Artifact Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.GCP_REGION }}-docker.pkg.dev
          username: _json_key
          password: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}

      - name: Get short SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITEA_OUTPUT

      - name: Determine tags
        id: meta
        run: |
          SHA=${{ steps.vars.outputs.sha_short }}
          REGISTRY=${{ secrets.HARBOR_URL }}
          PROJECT=zyntra
          TAGS="$REGISTRY/$PROJECT/marketing-frontend:dev-latest,$REGISTRY/$PROJECT/marketing-frontend:dev-$SHA"
          echo "tags=$TAGS" >> $GITEA_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          provenance: false
          build-args: |
            VITE_API_URL=${{ secrets.VITE_API_URL }}

      - name: Mirror dev-latest → GCP Artifact Registry
        run: |
          SOURCE="${{ secrets.HARBOR_URL }}/zyntra/marketing-frontend:dev-latest"
          TARGET="${{ secrets.GCP_REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/${{ secrets.GCP_REPOSITORY }}/marketing-frontend:dev-latest"

          docker pull $SOURCE
          docker tag $SOURCE $TARGET
          docker push $TARGET

          echo "✅ Mirror dev → $TARGET"

      - name: Trigger Dokploy Deployment
        run: curl -X POST "${{ secrets.DOKPLOY_WEBHOOK_URL }}"